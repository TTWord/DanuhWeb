import iconBackButton from '@/assets/svg/icons/icon-back-button.svg';

const Home = () => {
  return (
    <div>
      <img src={iconBackButton} alt="back" />
    </div>
  );
};

export default Home;

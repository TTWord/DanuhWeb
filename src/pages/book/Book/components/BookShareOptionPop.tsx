import ReactModal from 'react-modal';
import styled from 'styled-components';

const BookShareOptionPop = () => {
  return <ReactModal isOpen={false}></ReactModal>;
};

export default BookShareOptionPop;
